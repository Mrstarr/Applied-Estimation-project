
function tmp = find_iMat_idx(num, str)
    % get indices of the info matrix
    % str: 'x': pose; 'm': maps
    if str == 'x'
        % use num
    end
    
    if str == 'm'
        %
    end
    
    % note: should return a (x, 1) matrix
end